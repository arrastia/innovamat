import styled from 'styled-components';

const View = styled('div')`
  overflow: hidden auto;
  padding: 1rem;
  position: relative;
  width: 100%;
`;

const Title = styled('h2')`
  margin-bottom: 1.5rem;
`;

export const Styles = { Title, View };
